%h1 Latest books

- @books.each do |b|
  = link_to book_path(b), class: "nodecor" do
    %h3
      = succeed(',') do
        = b.title
      %i= b.author
  %p
    = markdown b.comment
    = surround('(',')') do
      %i= link_to b.user.nickname, user_path(b.user)
  
